# Makefile for the Synth plugin

# Include the template from the examples directory
include ../Makefile.template

# Plugin-specific configuration
PLUGIN_NAME := synth
PLUGIN_ID := com.clapgo.synth

# Additional flags
CFLAGS += -DCLAPGO_PLUGIN_ID=\"$(PLUGIN_ID)\"

# Default target
all: $(PLUGIN_CLAP)

# Custom targets can be added here